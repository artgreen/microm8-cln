# microM8 MCP browser tools

Start microM8 with MCP streaming enabled:

```sh
./microM8 -mcp -mcp-mode streaming -mcp-port 1983 -offline
```

Open `microM8-mcp-tool-lab.html` to inspect and run MCP tools.

Open `microM8-mcp-control.html` to control a running microM8 instance.
