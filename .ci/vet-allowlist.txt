# This file is no longer consulted by tools/scripts/check.sh — as of Phase 4c
# the vet step runs on `./...` directly (with three specific checks disabled;
# see check.sh for rationale). Kept for historical reference / rollback.
#
# Quarantined packages are excluded automatically by Go's `./...` traversal
# (they live under directories starting with `_`).
